version : 0.1

phases:
  post_build:
    - echo Build completed on `date`
  build:
    commands:
      - echo Nothing to do yet
  artifacts:
    files:
      - index.html
      - favicon.ico
      - styles/main.css
      - images/*
